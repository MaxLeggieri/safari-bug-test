import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'safari-bug-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
